name: CI

on:
  pull_request:
  push:
    branches: [work, main]

jobs:
  baseline-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Repository baseline
        run: |
          echo "Add project-specific checks here (lint/test/build)."
